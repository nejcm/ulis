{
  "$comment": "Per-repo overrides for src/config.ts BUILD_CONFIG. Deep-merged on top of code defaults — only specify the leaves you want to change.",
  "platforms": {
    "codex": {
      "trustedProjects": {}
    },
    "opencode": {
      "readAllowlist": {}
    }
  }
}
