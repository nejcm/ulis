# yaml-language-server: $schema=./node_modules/@nejcm/ulis/dist/schemas/plugins.schema.json

claude:
  plugins:
    - name: frontend-design
      source: official
    - name: ralph-loop
      source: official
